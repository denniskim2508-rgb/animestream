// Admin access is granted by email allowlist. Add any email that should be
// able to reach admin-only pages (e.g. the provider health dashboard).
// Emails are matched case-insensitively after trimming.
export const ADMIN_EMAILS = [
  'denniskim2508@gmail.com',
]
